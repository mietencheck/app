# Optional: browser calls Nest at this origin (Nest must allow CORS for the site).
# Omit (recommended on Cloudflare): same-origin `/api-mietencheck` → Worker proxy.
# VITE_MIETENCHECK_API_BASE_URL=https://api.example.com

# Sanity CMS (defaults to te770b4o / production if omitted)
# VITE_SANITY_PROJECT_ID=te770b4o
# VITE_SANITY_DATASET=production
