# Production / preview builds: absolute origin of the Nest Mietencheck API (no trailing slash).
# Omit locally: `pnpm dev` uses same-origin `/api-mietencheck` via the Worker proxy.
# VITE_MIETENCHECK_API_BASE_URL=https://api.example.com
